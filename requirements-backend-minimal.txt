# Backend dependencies WITHOUT heavy ML packages (TensorFlow, sklearn)
# This includes EEG/LSL support but skips fall detection and soft biometrics

# Core Python packages
paho-mqtt==1.6.1
pyyaml==6.0.1
python-dotenv==1.0.0

# Web framework
flask==3.0.0
flask-cors==4.0.0

# Numpy for frame calculations (has pre-built wheels, installs quickly)
numpy==1.24.3

# EEG/LSL streaming (REQUIRED for Emotiv headset integration)
pylsl==1.16.2

# Database
sqlalchemy==2.0.23
alembic==1.12.1
psycopg2-binary==2.9.9
asyncpg==0.29.0

# API framework
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0

# Web sockets
websockets>=12.0

# Data handling
pyarrow

# Launcher dependencies
psutil==5.9.6
requests==2.31.0

# NOTE: This file EXCLUDES TensorFlow, sklearn, pandas, matplotlib, seaborn
# Those are only needed for fall detection and soft biometrics features
